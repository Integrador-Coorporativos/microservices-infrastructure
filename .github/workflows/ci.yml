---
name: CI Pipeline

on:
  push:
    branches: ["main","develop"]
  pull_request:
    branches: ["main","develop"]

jobs:
  validate:
    name: Validate YAML and docker-compose
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y docker-compose yamllint

      - name: Validate YAML files (não trava CI)
        run: yamllint . || echo "⚠️ yamllint encontrou problemas, mas o CI continua."

      - name: Validate docker-compose
        run: docker-compose -f docker-compose.local.yml config

  build_images:
    name: Build Docker images
    runs-on: ubuntu-latest
    needs: validate

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build images
        run: |
          if [ -d "./services" ]; then
            for srv in services/*; do
              if [ -f "$srv/Dockerfile" ]; then
                echo "Building image for $srv"
                docker build -t "app-$(basename $srv):latest" $srv
              fi
            done
          else
            echo "Folder './services' not found, skipping image builds."

  infra:
    name: Setup Docker Compose and test infrastructure
    runs-on: ubuntu-latest
    needs: build_images

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install PowerShell
        run: |
          sudo apt-get update
          sudo apt-get install -y wget apt-transport-https software-properties-common
          wget -q https://packages.microsoft.com/config/ubuntu/22.04/packages-microsoft-prod.deb
          sudo dpkg -i packages-microsoft-prod.deb
          sudo apt-get update
          sudo apt-get install -y powershell

      - name: Start infrastructure (Linux)
        run: |
          chmod +x ./up.ps1
          pwsh ./up.ps1 --build

      - name: Wait for services to start
        run: sleep 60  # maior timeout para garantir inicialização dos containers

      - name: Run infrastructure checks
        run: |
          chmod +x ./tests/check-infra.sh
          ./tests/check-infra.sh

  tests:
    name: Run application tests
    runs-on: ubuntu-latest
    needs: infra

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run tests (Node, Python, Go, Java — adaptar conforme sua stack)
        run: |
          if [ -f "package.json" ]; then
            npm install
            npm test
          elif [ -f "pytest.ini" ]; then
            pip install -r requirements.txt
            pytest
          else
            echo "Nenhum framework de teste encontrado, pulando."

  security_scan:
    name: Security Scan (Trivy)
    runs-on: ubuntu-latest
    needs: build_images

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Trivy
        uses: aquasecurity/trivy-action@master

      - name: Scan Docker images
        run: |
          if [ -d "./services" ]; then
            for srv in services/*; do
              if [ -f "$srv/Dockerfile" ]; then
                echo "Scanning image for $srv"
                trivy image "app-$(basename $srv):latest"
              fi
            done
          else
            echo "No services folder found, skipping scan."

  deploy:
    name: Deploy (manual)
    runs-on: ubuntu-latest
    needs: [ tests, security_scan ]
    if: github.ref == 'refs/heads/main'
    environment: production

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Placeholder deploy
        run: echo "Configurar deploy para Docker Swarm, Kubernetes ou servidor remoto."
